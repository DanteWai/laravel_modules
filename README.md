Модульная структура

```
php artisan make:module ModuleName --all 
создает контроллер, апи контроллер, модель, миграцию, роуты, виды, вью шаблоны

Вместо --all могут быть указаны отдельные параметры
--migration
--vue
--view
--controller
--model
--api
```
